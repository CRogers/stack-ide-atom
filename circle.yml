machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/stack-ide-atom/.docker-stack"
    - "~/stack-ide-atom/haskell/.cabal-sandbox"

test:
  override:
    - npm install
    - ./docker-gulp test
