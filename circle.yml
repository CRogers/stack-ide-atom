machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/stack-ide-atom/.docker-stack"
    - "~/stack-ide-atom/haskell/.cabal-sandbox"

test:
  override:
    - git submodule update --init --recursive
    - npm install
    - ./docker-gulp build-stack-ide
