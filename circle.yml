machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
    - "~/stack-ide-atom/.docker-stack"
    - "~/stack-ide-atom/haskell/.cabal-sandbox"

  override:
    - bash circleci-docker-cache.sh

test:
  override:
    - npm install
    - ./docker-gulp test
